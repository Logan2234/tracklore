{
  "name": "@tracklore/shared",
  "version": "0.6.0",
  "description": "Types, enums and DTOs shared between the Tracklore API and web app",
  "private": true,
  "license": "AGPL-3.0-only",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "clean:dev": "rm -rf dist node_modules",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  }
}
